Run the full content pipeline: compute page definitions, generate markdown pages, then QA check.

```bash
pnpm pipeline
```

Report what passed, what failed, and any warnings from the QA check output.
